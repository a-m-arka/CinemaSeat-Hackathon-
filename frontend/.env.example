# The app runs immediately with its restored frontend demo data.
VITE_API_MODE=mock

# To connect a real backend, use remote mode and set this one base URL.
# VITE_API_MODE=remote
# VITE_API_BASE_URL=http://localhost:3000/api
